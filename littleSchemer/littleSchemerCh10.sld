(include "littleSchemerCh1.sld")
(define-library (little-schemer ch10)
  (export lookup-in-entry)
  (import (scheme base)
          (scheme write)
          (little-schemer ch1))
  (begin
    (define lookup-in-entry
      (lambda (name entry entry-h)
        #f))))
                                               


