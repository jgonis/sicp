(include "littleSchemerCh1.sld")
(define-library (little-schemer ch7)
  (export set?)
  (import (scheme base)
          (scheme write)
          (little-schemer ch1))
  (begin
    (define set?
      (lambda (lat)
        #f))))



