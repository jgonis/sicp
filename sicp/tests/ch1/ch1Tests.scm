(define-library (tests ch1 ch1Tests)
  (export ch1-tests)
  (import (scheme base)
          (scheme write)
          (ch1 ch1)
          (srfi 78))
  (begin
    (define ch1-tests
      (lambda ()
        (check-reset!)
        (check-set-mode! 'report-failed)
        (check (eq? 13 (ex-1-3 1 2 3)) => #t)
        (check (eq? 13 (ex-1-3 1 3 2)) => #t)
        (check (eq? 13 (ex-1-3 3 2 1)) => #t)
        (check (eq? 13 (ex-1-3 3 1 2)) => #t)
        (check (eq? 13 (ex-1-3 3 2 2)) => #t)
        (check (eq? 13 (ex-1-3 2 3 2)) => #t)
        (check (eq? 13 (ex-1-3 2 2 3)) => #t)
        (check (eq? 18 (ex-1-3 3 3 3)) => #t)
        (check (j-abs -1) => 1)
        (check-report)
        (check-reset!)))))
